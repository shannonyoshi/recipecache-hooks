import React from 'react'
import RecipeCard from "./recipeCard"

const RecipeDashboard = () => {
  return (
    <div>
      {/* TODO: map over truncated recipes an return a recipe card for each */}
      <RecipeCard/>
    </div>
  )
}

export default RecipeDashboard
